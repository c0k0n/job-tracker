I now have all the information needed. Let me compile my findings.
Findings: Technical Debt in the job-tracker SvelteKit Project
package.json
1. package.json:12-13 — check and preview/deploy scripts invoke wrangler directly (e.g. wrangler types --check, wrangler dev) rather than bunx wrangler. AGENTS.md states "wrangler is via bunx (no global install)." They currently resolve through node_modules/.bin since wrangler is in devDependencies, so this works, but it contradicts the documented convention and would break if node_modules/.bin weren't on PATH. — low — Prefix with bunx for consistency with AGENTS.md and robustness.
2. package.json:34 — typescript: ^6.0.3 is a caret-pinned major. TypeScript 6.x is a significant major release; typescript-eslint (^8.69.0) and svelte-check peer constraints may lag behind it, so a future install could resolve to an incompatible 6.x minor. — low — Confirm typescript-eslint/svelte-check support for TS 6 or pin more tightly, and keep bun.lock committed (it is).
3. package.json — No better-auth or drizzle-orm/@cloudflare/d1 deps yet (expected, pre-backend), but there is also no test runner and no test script, despite the codebase carrying many unit-testable pure helpers (kpis.ts, sortFilter.ts, money.ts, dates.ts) whose comments explicitly promise testability ("trivially unit-testable"). AGENTS.md confirms "No tests" is a known state. — low — This is acknowledged; note only that adding bun test later is a gap when the pure functions are already isolated for it.
vite.config.ts
4. vite.config.ts:11-13 — The runes forcing function is correct and matches AGENTS.md, and the "Can be removed in svelte 6" comment is future-looking (fine). No debt here. — (no finding)
tsconfig / eslint / prettier
5. eslint.config.mts:47-51 — Dead empty block { // Override or add rule settings here ... rules: {} } at the end of the config does nothing. — low — Delete the empty config object (or actually populate it).
6. eslint.config.mts:16 — .svelte-kit/ is ignored only via globalIgnores(['.svelte-kit/']), while worker-configuration.d.ts is not globally ignored (only prettierignored). This is the root cause of the 2 pre-existing Unused eslint-disable directive warnings described in AGENTS.md:20 — worker-configuration.d.ts gets linted and its embedded /* eslint-disable */ (lines 1, 30) plus inline // eslint-disable-line (lines 10631, 10648) report "no problems were reported." — medium — Add worker-configuration.d.ts to the eslint globalIgnores array (or a files override setting 'no-warning-comments': 'off'), which also removes the "pre-existing noise" from bun run lint.
7. tsconfig.json / .svelte-kit/tsconfig.json — Root tsconfig.json extends .svelte-kit/tsconfig.json, a generated file that does not exist until svelte-kit sync runs. The prepare script runs svelte-kit sync || true, so a fresh clone hitting tsc/build before prepare would fail; the || true also silently swallows sync failures. — low — Acceptable given SvelteKit convention; consider dropping || true so sync errors surface.
wrangler.jsonc / Cloudflare readiness
 8. wrangler.jsonc — No compatibility_flags: ["nodejs_als"] present. AGENTS.md:21 explicitly warns that adding Better Auth / D1 libs that pull in node:crypto/node:async_hooks will require this flag, and nothing in the codebase needs it yet. This is a known pending requirement, not a bug — flag it so the backend migration includes it. — low (forward-looking) — Add compatibility_flags: ["nodejs_als"] when Better Auth/D1 lands (not before).
 9. wrangler.jsonc:4 — compatibility_date is 2026-08-28, matching the generated worker-configuration.d.ts (workerd@1.20260903.1 2026-08-28). No inconsistency. (no finding)
10. wrangler.jsonc:6-9 — assets.binding: "ASSETS" plus main points at .svelte-kit/cloudflare/_worker.js; the generated Env in worker-configuration.d.ts exposes ASSETS: Fetcher. Consistent. No D1/KV/R2 bindings (expected). (no finding)
app.html / app.d.ts
11. app.html:6 — <meta name="text-scale" content="scale" /> is a nonstandard/unrecognized meta tag that appears in no framework template and no token/research; looks like accidental/leftover cruft. — low — Investigate and remove if not intentional.
12. app.html / src/error.html — Neither the app shell nor the static error page sets a theme-color meta (only color-scheme), even though the app ships an explicit light/dark palette in layout.css. Missing PWA/browser-chrome theming metadata. — low — Add <meta name="theme-color"> (and ideally a per-scheme variant) to both app.html and src/error.html.
13. app.html — %sveltekit.head% and <html lang="en"> present; favicon is injected via the layout rather than the shell (fine). No canonical/og: tags — defensible for a private, noindex-only app. (no finding)
14. src/app.d.ts:20-25 — Divergence from AGENTS.md:24. AGENTS.md states "src/app.d.ts: only App.Platform ... is declared. Add App.Locals when auth lands," but the code already fully declares App.Locals (with user: SessionUser | null), App.Error, and App.PageState. The root conventions doc is stale relative to the code. — medium — Update AGENTS.md:24 to reflect that App.Locals (+ Error, PageState) are already declared; only Better Auth's session field is pending.
15. src/app.d.ts:16-18 — App.Error.code?: string is declared with a comment stating it "lets routes branch (e.g. 404 vs 500) without parsing the message string." However, +error.svelte branches only on page.status, and no call site passes a code (the only App.Error producer is error(404, ...) in [...path]/+page.ts, which sets no code). The declared optional code field is currently dead/unexercised. — low — Either wire code usage in +error.svelte/a handleError, or trim the comment to avoid implying a feature that isn't wired.
16. src/app.d.ts:30-37 — App.PageState (appId/newApp/resume) is declared, but the actual dashboard uses URL query params (?app=, ?new=, ?resume=) processed via page.url.searchParams, and ApplicationDetailModal even exposes a global window.__openAppDetail using pushState('', { appId }). The PageState fields are never read via page.state and appId is effectively dead (the modal reads the URL, not state). — low — Align PageState with the real URL-driven mechanism or remove the unused fields/comment.
hooks.server.ts / auth stub
17. src/hooks.server.ts:26-29 — if (user) event.locals.user = user but the else branch does not clear locals.user. Because locals is fresh per request in SvelteKit this is safe today, but it's a fragile stub pattern — when the real auth lands, a session-lookup failure must explicitly null it. — low — Prefer unconditional event.locals.user = getSession(...) to be robust against the auth swap.
18. src/lib/server/auth.ts:18,31,56-62 — The stub session uses a plaintext JSON cookie (job-tracker.session) with secure: false and no signature. This is intentional and documented as a stub, but it's a security-relevant placeholder that must not ship to production as-is (any client can forge {"id":"stub-user-id"} to become the admin, since ADMIN_ID='stub-user-id'). — medium (forward-looking) — Keep as stub only; ensure Better Auth's signed/secure session fully replaces it and add an explicit reminder that the admin identity is trivially forgeable until then.
19. src/routes/+page.server.ts:86 — The auth action always throw redirect(303, '/dashboard'), ignoring the ?next= param that the dashboard guard writes (dashboard/+page.server.ts:38 redirects to /?next=...). A user redirected to / to sign in is sent to /dashboard (not back to their original page), and next is never read anywhere. — medium — Read next from the URL and redirect to it (validate it's an internal path) after successful auth, or remove the next-redirect in the dashboard guard.
Error handling
20. [src/routes/...path/+page.ts:11-12] — The doc comment claims the catch-all "still works even when individual routes also define a [...path] boundary for nested sections," but no such nested catch-all exists and a nested [...path] would conflict with the root one unless the root drops it per-route. The comment over-promises current behavior. — low — Soften/clarify the comment to reflect the actual single root catch-all.
21. src/routes/+error.svelte:22 — +error.svelte adds robots noindex (good) but the non-SSR-fallback src/error.html (which renders only when +error.svelte can't load / SSR failure) has no noindex and no theme-color. Both also lack a lang mismatch check (both are lang="en", fine). — low — Add <meta name="robots" content="noindex"> and theme-color to src/error.html for parity.
22. src/routes/+error.svelte:30-32 — The error page marks up the eyebrow "Error {status}" without a prefers-reduced-motion/semantic concern — actually the page is well-formed (main with aria-labelledby, focus management via hard nav). The two button actions duplicate each other's function (window.location.assign('/') always works while history.back() can leave the noindex page / go nowhere), but both are defensible. (no finding)
23. src/routes/dashboard/+page.server.ts:337-345 & src/lib/types / components — The getDetail named action is dead code: its docstring says "Stub for future ... Kept so future 'fetch on hover' hooks have a clean target," but it is never posted to by any form or called anywhere (the detail modal is server-loaded via the ?app= load, per load's comment). — low — Remove the getDetail action until actually used, or wire it up.
Dead/unused code & minor
24. src/lib/utils/kpis.ts:76-95 — countByStatus and needsAttentionList are exported but never imported anywhere (only computeKpis/isStageOpen are used by dashboard/+page.server.ts). Their comments say "Not used in Round A" / "Used in Round A's empty-state copy" but the empty-state copy doesn't call them. — low — Remove or mark clearly as intentionally-future and add a usage note when consumed.
25. src/lib/server/applications-data.ts:706-709 — resetStore() is exported but never called from app code; it exists for tests/Playwright determinism (required if tests are added). Currently dead in the shipped app. — low — Keep (it's test-oriented); optionally note it's test-only.
26. src/lib/components/ApplicationDetailModal.svelte:69-77, 107-111 — Two stub/global globals: window.__openAppDetail window-side escape hatch (documented as "tiny, scoped escape hatch") is defined but nothing in the dashboard calls it (the dashboard uses goto/onRowClick, not __openAppDetail), making it dead. Also RESUME_URLS maps fixture resume IDs to third-party sample PDFs (w3.org, africau.edu, orimi.com) — external mock data that will break/change and is a real divergence from "R2 upload lands in backend round." — medium — Remove __openAppDetail (unused) and localize/externalize the mock PDF URLs or clearly gate them behind a stub flag.
27. src/lib/components/ResumeLibraryModal.svelte:167-179 — Upload button is disabled yet still has an onclick that sets an error string; disabled buttons don't fire clicks, so the error message is unreachable via that control. The whole upload flow is a documented stub ("pending backend round"). — low — Remove the dead onclick on the disabled button (or enable it), since the disabled state makes the handler unreachable.
28. src/lib/components/ConversionFunnel.svelte:145-147 / 130-136 — Duplicated/slightly inconsistent conversion math: the conv computed at lines 131-135 (conv === null ? 0 : ...) is recomputed in the tooltip builder at lines 146-147 ((conv ?? 0)), and isFirst/prevCount interplay is convoluted. Not a bug, but a readability smell. — low — Consolidate the conversion derivation into one $derived.
Cross-cutting / conventions
29. vite.config.ts vs README/AGENTS — No debt; config matches AGENTS conventions (runes forced, adapter in vite.config, no svelte.config.js). (no finding)
30. README.md — Empty (0 lines). AGENTS.md acknowledges this as intended pre-feature, but for a deployable Cloudflare Worker with multiple rounds of features it's now stale/absent documentation. — low — Fill in setup, commands, and the stub-DB/Better-Auth migration notes.
31. src/app.d.ts:3 + slots — import type { SessionUser } from '$lib/server/auth' inside app.d.ts pulls a $lib/server module into the ambient types. This is type-only, so it doesn't cause the build-blocking client-import error AGENTS warns about, but it does couple the global App types to a server-only module's shape; when Better Auth lands the type will need churn. — low — Fine for now; note that SessionUser's shape will be replaced by Better Auth's exports.
Severity summary
- Medium: #6, #14, #18, #19, #26
- Low: #1, #2, #3, #5, #8, #11, #12, #15, #16, #17, #20, #21, #23, #24, #25, #27, #28, #30, #31
The most actionable items before the D1/Better Auth backend round are #18 (forgeable admin cookie — must be fully replaced, never shipped as-is), #19 (?next= redirect is written but never honored), #14 (AGENTS.md staleness), #6 (make worker-configuration.d.ts eslint-clean), and #8 (remember compatibility_flags: ["nodejs_als"] when the backend lands).

---
Technical debt report — job-tracker
1. Dead code (zero references outside definition)
 1. src/lib/utils/dates.ts:26 — daysBetween exported, no callers anywhere in src/ — low — delete, or use it in StageDwellChart/dwell math instead of daysSince.
 2. src/lib/utils/dates.ts:118 — toDateInputValue exported, zero callers — low — delete, or wire it into the appliedAt <input type="date"> default.
 3. src/lib/utils/kpis.ts:76 — countByStatus exported, zero callers (doc admits "future round") — low — delete until the status-breakdown card exists.
 4. src/lib/utils/kpis.ts:91 — needsAttentionList exported, zero callers; doc claims it was "used in Round A's empty-state copy" which doesn't exist — medium — delete; the dashboard empty-state shows generic copy and staleCount is computed separately.
 5. src/lib/types.ts:186 — TrashView type, zero references (trash is handled by a ?trash=1 string, not this type) — low — delete.
 6. src/lib/utils/sortFilter.ts:215 — hasActiveFilters exported but never imported; both consumers (FilterBar.svelte:92, dashboard/+page.svelte:93) define their own local $derived copy — low — delete the export; the predicate is triplicated.
 7. src/lib/server/applications-data.ts:488 — getApplicationById exported, zero callers (detail goes through getApplicationDetail) — low — delete or use inside getApplicationDetail.
 8. src/lib/server/applications-data.ts:706 — resetStore exported, zero callers; the HMR store can never be reset without writing code — low — delete, or export a dev-only reset endpoint/hook.
 9. src/lib/server/applications-data.ts:711 — export type { CurrencyCode } re-export, nothing imports it from this module — low — delete; import from $lib/types at the consumer.
10. src/lib/components/Modal.svelte:12,15 — module-scope exports getOpenCount/getTopId never consumed anywhere — low — delete both.
11. src/lib/components/ApplicationDetailModal.svelte:107-111 — window.__openAppDetail + pushState('', { appId }) is dead: the dashboard row click uses goto('?...app=') (dashboard/+page.svelte:133-141), and page.state is never read — low — delete the global function, the pushState import, and the App.PageState.appId field.
12. src/app.d.ts:16-18 — App.Error.code? documented but no error() call ever passes a code — low — delete or populate.
13. src/app.d.ts:33-37 — App.PageState (appId/newApp/resume) never read; modals are URL-query-driven (?app=/?new=/?resume=) instead of shallow-routed via state — low — delete the interface and fix the stale comments that claim "Round C uses shallow routing".
14. src/lib/server/applications-data.ts:423 — comment promises deterministic seeding "so the Playwright tests can assert specific counts"; no tests exist in the repo — low — drop the Playwright reference.
2. Configured but not usable in the UI (data-model features with no reachable trigger)
15. src/lib/types.ts:60-64, src/lib/constants/currencies.ts:29-279, src/lib/utils/money.ts:58 — the entire Salary/currency model (32 codes, CurrencyMeta.label per currency, 4 salary shapes, formatSalary) is display-only: create hardcodes salary: null (+page.server.ts:177), edit never touches salary, and ApplicationForm has no salary/currency/amount fields — high — add a salary input (shape + amounts + currency select) to the form and create/edit actions, or strip the model down to fixtures-only.
16. src/routes/dashboard/+page.server.ts:223 — edit action unconditionally writes nextActionAt: null, wiping the fixture value on the first edit; there is also no form field that sets nextActionAt even though it drives a table column, a sort key, and the "Interviews next 30 days" KPI — high — remove the hardcoded null (let it stay unchanged) and add a "next action" date field.
17. src/routes/dashboard/+page.server.ts:179-180,182 — postingDescription, application-level notes, and resumeId are never settable via UI: no form fields exist, create hardcodes the first two to null, and the resume upload button is disabled ("pending backend round") — medium — add the fields to ApplicationForm/edit, or drop them from the create payload.
18. src/routes/dashboard/+page.server.ts:294-298 — addInterview hardcodes durationMinutes, withRole, and outcome to null; the form has no inputs for them, so outcome in the UI (ApplicationDetailModal.svelte:439) always renders "pending" — medium — add inputs or remove the fields from the model.
19. src/routes/dashboard/+page.server.ts:326 — addContact hardcodes phone: null and never sets company; Contact.phone and Contact.company are never rendered in the contacts tab — medium — add inputs or drop the fields.
20. src/lib/types.ts:167-169 — ActivityEvent.kind includes 'note_added' | 'status_changed' but no code path ever produces those events (only created, stage_changed, interview_scheduled, contact_added are pushed) — low — remove the unreachable union members or emit them from updateApplication.
21. src/lib/constants/currencies.ts:22 — CurrencyMeta.label (32 entries) is never read anywhere; CSV and formatter use codes/symbols only — low — remove label or use it in a future currency picker.
22. src/routes/dashboard/+page.server.ts:337-345 — getDetail action exists with no UI form posting to ?/getDetail (its own doc admits it's a placeholder); it's also the only action returning a bare object instead of fail/redirect, and its fails omit the operation field every other action includes — low — delete it or wire it to an actual "fetch on hover" hook.
23. src/lib/types.ts:138 + ApplicationDetailModal.svelte:385-391 — interview scheduledAt comes from <input type="datetime-local"> (no timezone, no Z), then round-trips through new Date() for sorting/formatting; stored value isn't a real ISO string and parses differently per runtime TZ (dev machine vs Workers UTC) — medium — convert to UTC ISO on submit (append :00Z / Time.from), or store as-is and document the convention.
3. Stub / HMR hacks
24. src/lib/server/applications-data.ts:37,396-417 — globalThis + Symbol.for module store: state survives HMR but on Workers the map is per-isolate, so (a) user-created rows are lost when the isolate is recycled, (b) multiple isolates diverge, and (c) the store silently persists across requests with no capacity bound; a real D1 swap is the only true fix — medium — while the stub remains, document isolate semantics in the header and scope a migration ticket; do not extend the stub further.
25. src/lib/server/applications-data.ts:397 — globalThis as unknown as Record<symbol, Store | undefined> cast + delete g[STORE_KEY] in resetStore; the whole HMR survival trick is untestable without a reset call site (see #8) — low — replace with a module-scope let store + injected reset for tests.
26. src/routes/+page.server.ts:76-84 — stub "sign-in succeeds for any credentials" sets a session for literally any input; setSession also happily accepts a user with a non-string email — low — documented stub; add the real Better Auth call before any real data lands.
4. Doc comments contradicting behavior / return values ignored
27. src/lib/components/ApplicationDetailModal.svelte:45 — const next = (qs ? /dashboard?\${qs} : '/dashboard') as /\${string}; contains real backslash bytes (\${qs}, \${string} — verified with od -c), so the string is a literal ${qs}, not an interpolation: closing the detail modal while any other query param is present (?trash=1, filters, ?new=1) navigates to a garbage /dashboard?${qs} URL, dropping the user's view state — high — remove the backslashes (match the correct pattern in ResumeLibraryModal.svelte:32 and dashboard/+page.svelte:160).
28. src/routes/dashboard/export.csv/+server.ts:9-10,72-93 — doc claims stage/status/arrangement/salary use "canonical display labels" but the CSV emits raw enum codes (phone_screen, active) and raw minor-unit integers — medium — either emit STAGE_BY_VALUE/STATUS_BY_VALUE labels or fix the doc.
29. src/lib/utils/dates.ts:15-21,36-53 + ApplicationsTable.svelte:277-278 — daysSince floors future dates to 0, so formatRelative(nextActionAt) renders any future "next action" date as "today" — medium — add a future branch to formatRelative ("in 3 days") or compute against daysBetween.
30. src/routes/dashboard/+page.server.ts:113-115 — comment claims modals close because the redirect "clears page.state which closes the shallow-routed modal"; modals are actually URL-query driven and close because the redirect drops ?app=; page.state is never used — low — fix the comment to describe the actual ?app= mechanism.
31. src/routes/dashboard/+page.svelte:101-103 — staleCount recomputes stalled+ghosted client-side even though data.kpis.needsAttention (kpis.ts:57-58) already provides the identical count — low — derive the banner from kpis.needsAttention.
5. Type dodge / non-null assertions / unvalidated casts
32. src/lib/utils/sortFilter.ts:183 — sortKey as ApplicationSort['key'] casts any URL ?sort= value without validating against the SortKey union; ?sort=bogus silently round-trips and breaks sorting — medium — validate against SortKey and fall back to 'stageChangedAt'.
33. src/lib/utils/sortFilter.ts:159-165 — arrangements are only .filter(Boolean) before being cast to ApplicationFilters['arrangements']; ?arr=bogus produces a filter that silently matches zero rows (stage/status at least validate against their order maps) — medium — filter against ARRANGEMENTS values before casting.
34. src/routes/dashboard/+page.server.ts:174-176 — stage!, status!, workArrangement! non-null assertions after manual validation — low — return a narrowed object from a validation helper instead of asserting.
35. src/lib/components/FilterBar.svelte:115 — TAG_BUCKETS[0]! non-null assertion on a constant array — low — use ?? 0 or index with a length guard.
36. src/lib/components/Modal.svelte:91-92 — tabbables[0]! / tabbables[tabbables.length - 1]! non-null assertions (safe only because of the preceding empty check) — low — use ?? first/?? last fallbacks.
37. src/lib/server/auth.ts:41 — JSON.parse(raw) as SessionUser validates only id/username are strings; a hand-crafted cookie with a non-string email passes the guard and flows into App.Locals.user — low — validate all three fields (or email optionally).
38. src/routes/dashboard/+page.server.ts:75-78,202-204 — pickEnum returning null is silently swallowed in edit (stage ?? undefined), so an invalid stage/status/arrangement value in the edit POST is ignored rather than rejected, while create fails 400 on the same input — medium — fail 400 on invalid enums in edit too.
6. Error handling / form plumbing
39. src/routes/admin/approvals/+page.server.ts:66-83 — approve/reject actions perform no auth check at all (the admin gate lives only in load); a logged-out or non-admin POST succeeds; empty/missing id is also accepted with no fail() — medium — re-check isAdminUser(event.locals.user) in each action and fail(400) on missing id.
40. src/lib/components/ApplicationForm.svelte:32-45 + src/routes/dashboard/+page.svelte:23 — the form's values/errors/operation props are never passed by any caller (<ApplicationForm create /> at +page.svelte:425, <ApplicationForm {application} /> at ApplicationDetailModal.svelte:593), and dashboard/+page.svelte destructures only data (never form); all the fail(400, { operation, values, errors }) payloads from create/edit are silently swallowed — invalid submissions produce zero feedback and the submit button never shows a busy state (operation is always undefined) — high — thread form from the page into ApplicationForm (<ApplicationForm values={form?.values} errors={form?.errors} operation={form?.operation} />) or render the errors in the modal.
41. src/lib/components/ApplicationForm.svelte:88-94 vs src/routes/dashboard/+page.server.ts:143-149 — tag normalization diverges: the client replaces whitespace with hyphens on blur, the server only lowercases; a JS-less submit leaves space-containing tags that then never match filter chips — low — mirror the hyphen normalization server-side.
Highest-priority fixes (the three that change observable behavior today): #27 (escaped template literal breaks modal-close navigation), #16 (editing wipes nextActionAt), and #40 (validation errors invisible). The dead-code cluster (#1–#14) is safe to remove mechanically; #15/#22/#38 are the "configured but unreachable" features that need a product decision (wire up or cut).

---
Technical Debt Report — Job Tracker (SvelteKit / Svelte 5 runes / Tailwind v4)
I verified every claim against source. svelte is at ^5.57.0 (writable $derived supported), so bindable-derived modals are valid — I do not flag those as bugs, but note the fragility.
1. Unused imports, unused runes, dead variables/exports
- src/lib/components/ApplicationDetailModal.svelte:115-116 void STAGES; void STATUSES; — Comment claims these keep noUnusedLocals honest, but both STAGES (line 79) and STATUSES (line 80) ARE used. Dead statements, stale workaround. — low — "Delete the void STAGES; void STATUSES; lines; the imports are already used at lines 79-80."
- src/lib/components/Modal.svelte:12-17 export function getOpenCount() / export function getTopId() — module-level exports never imported anywhere in the codebase (grep confirms only definitions). Dead API + dead module-scope counters openCount/topId logic that only exists to serve them. — low — "Remove the two exported getters (and simplify the counter bookkeeping if nothing else consumes it)."
- src/lib/components/ApplicationDetailModal.svelte:107-111 window.__openAppDetail registration — a global escape hatch assigned on window but never invoked anywhere (grep finds only the definition). Dead globals-polluting code masquerading as a pattern; the dashboard row actually calls onRowClick → goto, not this. — medium — "Delete the window-global registration; the row-open path already flows through the onRowClick prop."
- src/lib/utils/dates.ts:26 daysBetween() — exported, zero call sites. — low — "Remove or mark it used."
- src/lib/utils/dates.ts:118 toDateInputValue() — exported, zero call sites (ApplicationForm reimplements appliedAt.slice(0,10) instead). — low — "Remove, or use it in ApplicationForm."
- src/lib/utils/kpis.ts:76 countByStatus() — exported, zero call sites ("not used in Round A but defined now"). — low — "Drop until a consumer exists."
- src/lib/utils/kpis.ts:91 needsAttentionList() — exported, zero call sites. — low — "Remove unused export."
- src/lib/components/ApplicationForm.svelte:38-42 props values, errors, operation — accepted by the component but no caller passes them (both call sites are <ApplicationForm create /> and <ApplicationForm {application} />). Server actions DO return {operation, values, errors} (dashboard +page.server.ts:164-168, 200, 226), so the entire server-validation echo surface is dead in the UI. — high — "Pass form into ApplicationForm (and forward data.form from the page) so validation errors and the busy state actually surface."
- src/lib/components/ApplicationForm.svelte:96-99 formError and isBusy $deriveds — both only ever evaluate to falsy/false given the unwired props above. Effectively dead logic until (and unless) the props are passed. — medium — "Wire the props (see previous item); otherwise the error/skeleton affordances can never fire."
- src/routes/dashboard/+page.svelte:93-99 hasActiveFilters $derived re-declared locally, duplicating both FilterBar.svelte:92 and the existing shared hasActiveFilters() in sortFilter.ts:215 (which the page imports from but does not use for this). Triple duplication of one predicate. — medium — see Axis 3/duplication.
2. Disabled / "coming soon" / placeholder affordances; lying aria-labels
- src/lib/components/ResumeLibraryModal.svelte:167-179 Upload button is disabled but still has an onclick that sets a fake error message. A disabled button does not fire onclick, so the "Upload is wired in the backend round…" feedback can NEVER display. The button is permanently inert with a handler that can't run and an ariaLabel ("Upload resume (pending backend round)") that advertises an unimplemented action. — high — "Either drop disabled and let the click show the informational message, or remove the dead handler."
- src/lib/components/ResumeLibraryModal.svelte:147 "Stored in R2 once the backend round lands." — copy describing unimplemented behavior; the whole modal is a stub. — low — "Scope copy to what works today or mark the modal as a preview."
- src/routes/admin/approvals/+page.svelte:42,50,118-119 Approve/Reject "wired in the backend round… for now they log to the server console" — buttons work but perform no mutation; the role="status" message says "Pending backend wiring." Legitimate stub, but the action is a no-op the user can't distinguish from success. — low — "Clearly label actions as non-persistent (e.g. an explicit 'This is a preview — nothing is saved')."
- src/routes/dashboard/+page.svelte:407 footer "data is stubbed until the backend round" — honest, fine. (Not flagged as debt; noting only.)
- src/lib/components/ApplicationForm.svelte:255-262 Submit ariaLabel and visible text are always identical ("Create application"/"Save changes"), so the ariaLabel is redundant — but more importantly it can never show the busy/"Saving…" text because isBusy is never true. See Axis 1.
3. Duplicated component patterns / inconsistent props APIs
- src/lib/components/FilterBar.svelte:170-187, 195-213, 220-237 Three nearly-identical chip groups (Stage / Status / Work) — same button markup, same active/!active class permutations, same toggleX reducers (lines 43-65). Tag + Sort chips (251-271, 281-304) are a fourth/fifth variant with slightly divergent classes (hover:bg-surface-2 vs hover:opacity-80). — medium — "Extract a <FilterChip active onToggle> component (and a shared toggleIn(filters, key, value) helper) to collapse the ~5 near-copies and the hover: inconsistency."
- src/lib/components/FilterBar.svelte:92-98 / src/routes/dashboard/+page.svelte:93-99 hasActiveFilters computed in two components AND available as a shared util (sortFilter.ts:215). — medium — "Import hasActiveFilters from sortFilter in both places; drop the local $deriveds."
- src/lib/components/ConversionFunnel.svelte:32-49 / src/lib/components/StatusBadge.svelte:26-53 Two hand-maintained colorToken → Tailwind class maps (STAGE_BG/STAGE_TEXT and BG_BY_TOKEN/TEXT_BY_TOKEN) with overlapping keys and near-identical literal lists. Duplicated, drift-prone. — medium — "Consolidate the token→class map into one shared module (both consume colorToken)."
- src/lib/components/ApplicationDetailModal.svelte:69-76 / src/lib/components/ResumeLibraryModal.svelte:44-51 Identical RESUME_URLS mock maps (same 6 keys + same external PDF URLs) duplicated verbatim. — medium — "Hoist RESUME_URLS to a shared $lib/constants/resumes.ts."
- src/routes/dashboard/+page.svelte:327-337, 373-376 and src/routes/admin/approvals/+page.svelte:21-26 Border-style anchor "buttons" (Export CSV / Admin queue / Back-to-active / ← Back) hand-rolled with the same long class strings, while the codebase already has a Button component that renders <a>-like styling… but these are real <a> links so they don't use Button. — low — "Consider a Button as/link variant (or LinkButton) so link-buttons share the ghost/outline vocabulary instead of bespoke class strings."
- src/lib/components/StageDwellChart.svelte:78-87, 89-104 / src/lib/components/VelocityChart.svelte:135-151, 153-172 / src/lib/components/ConversionFunnel.svelte:101-120 Three near-identical "card with mono uppercase heading + empty state" shells (same rounded-lg border bg-surface px-4 py-5/6, same font-mono text-[11px] tracking-widest heading). — low — "Extract a ChartCard (heading, subheading, empty-state slot) shared by all three."
- src/routes/dashboard/+page.svelte:304-309, 327-337, 373-376 Recurring pill/border-link pattern (rounded-full border ... font-mono text-[11px] text-muted) for the trash-count badge and the export/admin links, none of which use a shared chip/link component. — low — "Share the pill/link styling (ideally via the FilterBar chip component)."
- src/lib/components/ApplicationsTable.svelte:291-306, 296-306, 328-354 / src/lib/components/ApplicationDetailModal.svelte:298-344 Both components independently implement the Restore / Move-to-trash / Delete-and-confirm-purge cluster inside <form use:enhance> wrappers with hidden id inputs — the exact same pattern, including the two-step purge confirm (confirmPurgeId vs confirmPurgeOpen). — medium — "Extract a shared <RowActions>/DeleteConfirmGrid component (and a SubmitForm wrapper for use:enhance + hidden id)."
4. Accessibility gaps
- src/lib/components/ApplicationsTable.svelte:186 A <tr> is given role="link" + tabindex="0" + Enter/Space key handler while the SAME row contains real interactive <button>s inside <form>s. A table row can't carry link semantics over nested interactive controls; it also adds a second tab stop per row (row + buttons) with a confusing ARIA announcement. — high — "Remove role="link"/tabindex from the <tr>; put onclick on a real row body cell or add a dedicated 'Open' control instead."
- src/routes/+page.svelte:75-108, 141-145 Both auth tabs declare aria-controls (auth-panel-signin/auth-panel-signup) but only ONE panel element exists at a time (its id is conditional), so the inactive tab's aria-controls points at an id not in the DOM. Also the tabs lack arrow-key (roving tabindex) navigation. — medium — "Render both panels (toggle hidden) so every referenced id exists, and add Left/Right arrow handling."
- src/lib/components/Modal.svelte:174-177 Backdrop comment says "inert … would also work" but inert is NOT applied — aria-modal="true" alone does not prevent tabbing/AT access to the background content. — medium — "Wrap the modal in inert when open (or set aria-hidden + focus-trap the background as intended)."
- src/lib/components/StageDwellChart.svelte:151-154, 127-130 Per-dot info is hover-only via title (dots inside an aria-hidden="true" track). Position/dwell data is only surfaced through the row-level aria-label, not per-dot, and never keyboard-accessible. — medium — "Add a data table or keyboard-reachable summary (e.g. a visually-hidden <dl> of live apps) instead of hover-only title."
- src/lib/components/VelocityChart.svelte:221-233 Per-day data is hover-only: transparent <rect><title> columns (~90 of them) with no focusable/keyboard equivalent. The <svg> has a <title> but individual day values are unavailable to keyboard/AT users. — medium — "Provide a table or aria-describedby summary of peak/notable days rather than hover-only rects."
- src/lib/components/ConversionFunnel.svelte:148-151, 186-190 Conversion rationale is hover-only (title/tooltip on the <li> and readout span); the aria-label on each <li> duplicates the tooltip but the row isn't focusable so it's never announced in practice. — medium — "Render the per-stage explanation as visible/presented text (or a details element) rather than hover-only."
- src/lib/components/ApplicationsTable.svelte:202-220 The "Resume attached" indicator is an icon-with-aria-label/title — acceptable for a non-interactive decoration, but it's a decorative icon given an aria-label (adds a spurious node to the AT tree). — low — "Use aria-hidden + a sr-only 'Has resume' text instead of aria-label on a decorative span."
- src/routes/+page.svelte:164, 193, 218 Inputs and submit duplicate the Button/input class string with disabled:cursor-not-allowed disabled:opacity-60 inline instead of a shared token; not an a11y bug per se but the redundancy risks drift (see Axis 5/9). — low.
- src/routes/dashboard/+page.svelte:277, 286, 297 Section aria-labelledby targets are fine, but the KPI strip section (line 244) uses aria-label "Application metrics" while StatCards use <h3> headings — heading order is <h3> inside a KPIs section with no <h2>, and the same <h3 class="font-mono…"> label styling is reused as both a visible label and (ambiguously) a heading. — low — "Make KPI labels non-heading text or promote to a proper heading hierarchy."
5. Inconsistent micro-interactions / motion / reduced-motion
- src/lib/components/* — no motion-reduce/prefers-reduced-motion anywhere despite animate-spin (Button.svelte:75, +page.svelte:250) and animate-pulse (StatCard.svelte:69) and transition-colors across ~30 sites. grep for prefers-reduced-motion|motion-reduce returns nothing. — high — "Add a global @media (prefers-reduced-motion: reduce) rule disabling animate-*/transition-* (*{animation:none;transition:none}) in layout.css."
- src/lib/components/FilterBar.svelte:176, 201, 226, 288 Active chips use bg-accent text-accent-fg while hover uses hover:bg-surface-2; but the tag chips (line 266) and sort chips (line 295) use hover:opacity-80 instead of hover:bg-surface-2. Same conceptual control, different hover affordance. — low — "Share one chip component so hover/active motion is uniform."
- src/lib/components/StatusBadge.svelte:71 / src/lib/components/ApplicationsTable.svelte:176 / src/routes/dashboard/+page.svelte:306 cursor-pointer is applied inconsistently (badge span, rows) while other clickable controls rely on default cursors; hover states differ (bg-border/40 on table sort buttons (ApplicationsTable:65) vs bg-surface-2 elsewhere). — low — "Standardize hover/cursor tokens for interactive surfaces."
- src/routes/+page.svelte:84,100 Both auth tabs and the submit button hand-roll transition-colors + focus ring, while Button.svelte already defines the exact same focus-visible vocabulary — the auth page never reuses Button. — medium — "Refactor the auth page submit + tabs to use Button/shared classes so motion/focus are consistent."
6. Hard-coded strings and repeated magic numbers
- src/lib/components/ResumeLibraryModal.svelte:100 10 * 1024 * 1024 (10 MB) is a magic number duplicated against the copy "Max 10 MB" on line 147 and in the error on line 101. — low — "Extract const MAX_UPLOAD_BYTES = 10 * 1024 * 1024 and drive the copy from it."
- src/routes/dashboard/+page.svelte:28-35, 53-68 All modal query flags ?app, ?new, ?resume, ?trash are bare string literals repeated across +page.svelte, ApplicationDetailModal, and ResumeLibraryModal (e.g. 'app', 'new', 'resume', 'trash=1'). — low — "Define QUERIES constants (app, new, resume, trash) in $lib/constants."
- src/lib/components/ApplicationsTable.svelte:49, 239, 247 TAGS_VISIBLE = 3 is a local magic constant; threshold for +N is arbitrary and not shared with the detail modal's tags rendering. — low — "Move to a shared constant if the truncation rule should be global."
- src/lib/components/StageDwellChart.svelte:30, 173-181 TICKS = [7, 14, 21] hard-coded; the "1w/2w/3w" note (line 111) and axis labels {tick}d duplicate the same semantic in two forms. — low — "Derive labels from TICKS."
- src/lib/components/VelocityChart.svelte:74-77, 12 W=800/H=180, windowDays=90, and the 6 label step are magic numbers, repeated with the 24*60*60*1000 math — note 24*60*60*1000 also appears in dates.ts (lines 20,29,101) and VelocityChart (43,58) as repeated day-ms arithmetic. — low — "Add a DAY_MS constant to dates.ts and reuse it."
- src/lib/components/FilterBar.svelte:37 Debounce 150 ms magic number (also referenced in a comment on dashboard +page.svelte:46). — low — "Extract DEBOUNCE_MS const."
- Arrow glyphs ↑/↓ hard-coded as Unicode with aria-hidden at ApplicationsTable.svelte:69,91,131,147,163 and FilterBar.svelte:299-301 — repeated string literals. — low — "Extract a <SortArrow dir> component or shared glyph constant."
7. Svelte 5 runes anti-patterns / event smells
- src/lib/components/ApplicationDetailModal.svelte:45 \${qs} and \${string} contain literal backslashes (verified via cat -A). Inside a template literal \${ is an escaped literal $, so next is /dashboard?\${qs} — the literal characters ${qs} are NOT interpolated. When the detail modal self-closes it navigates to a malformed URL instead of stripping ?app=. (The identical blocks in ResumeLibraryModal:32 and dashboard:160 use ?${qs} correctly.) — high — "Remove the stray backslashes:  `/dashboard?${qs}`  /  `/${string}` ."
- src/lib/components/ApplicationDetailModal.svelte:32 and src/routes/dashboard/+page.svelte:147, 420 The "writable $derived bound via bind:open" pattern (Svelte ≥5.25) mutates a derived value whose source is the URL — an intentional but fragile pattern; open and page.url can momentarily disagree, and the pattern is copy-pasted across 3 places. — medium — "Document the invariant, or route open/close through a shared useUrlModal() helper to avoid three divergent copies."
- src/lib/components/Modal.svelte:134-165 The open-lifecycle $effect writes module-scope openCount/topId and reads them in teardown; the comment correctly explains why they're non-reactive to avoid an infinite loop, but this is a hand-rolled global-stack lock that's only needed for the dead getters (Axis 1). — low — "Simplify once getOpenCount/getTopId are removed."
- src/lib/components/ApplicationsTable.svelte:177-196 Row-click uses the .closest('button, a, input, select, textarea') manual event-filter smell (a manual stopPropagation workaround) to avoid the row opening the modal when a nested button is clicked. Combined with role="link" this is fragile — any new interactive element must be remembered here. — medium — "Nest a real row-button/wrapper instead of filtering target selector lists."
- src/routes/+page.svelte:14-18 $effect that syncs mode from form?.mode — a prop-read effect with no teardown; acceptable (fires once) but is the ant-pattern that prop-reactivity side effects are discouraged; prefer deriving mode from $props. — low — "Drive mode from $derived(form?.mode) instead of a write-back effect."
- src/lib/components/ApplicationForm.svelte:52-85 init() reads props once via untrack at mount and stores in $state; the comment says the form remounts. Fine for now, but it means values/errors echo is ignored entirely (see Axis 1) — the design assumes a remount that itself is brittle under SvelteKit use:enhance resiliency. — medium (paired with Axis 1).
8. Invalid / non-semantic HTML
- src/lib/components/ApplicationsTable.svelte:186 role="link" on a <tr> containing buttons/forms = interactive container mis-role (covered in Axis 4) — the most egregious semantic violation. — high.
- src/lib/components/Modal.svelte:174-176 Backdrop div is role="presentation" with an onclick — semantically a presentation element with a gesture; acceptable pattern but it relies on e.target === e.currentTarget — fine. Not a violation. (Noting only.)
- src/routes/+page.svelte:131-139 input type="text" with aria-hidden="true" + class="sr-only" + tabindex="-1" used as a hidden username field. sr-only keeps it in the a11y tree shape but aria-hidden="true" on a focusable-off field is acceptable (it has tabindex=-1). This is the standard password-manager latch; acceptable. (Noting only.)
- src/lib/components/FilterBar.svelte:267-268, StageDwellChart.svelte:133,177 Inline style: attributes with dynamic arbitrary values (oklch inline colors in FilterBar tag chips; left %-positions) — not invalid HTML, but inline styles that bypass the token system (Axis 9).
9. Inline styles / arbitrary values that should be tokens; cosmetic drift
- src/lib/components/FilterBar.svelte:106-133, 267-268 The auto-hash tag palette emits inline style:background-color/style:color with hand-written oklch(...) values — the ONLY place in the app that bypasses layout.css tokens, and it duplicates the same oklch(0.95 0.02 <hue>) / oklch(0.42 0.12 <hue>) chroma magic used by the token palette. Hard-coded contrast risk: oklch(0.95 …) background with oklch(0.42 …) text is low-contrast in dark mode (tokens switch, inline styles don't). — high — "Define tag palette as theme tokens (or compute via the same token values) and set classes not inline styles, so dark mode swaps contrast correctly."
- src/lib/components/StatCard.svelte:37,45 tone="warning" maps to text-muted and accent="warning" maps to bg-muted — a text-color token reused as a background and a warning that is visually the same as neutral muted, so the warnings card reads as grey rather than a distinct state. — low — "Give warning its own bg/fg tokens (like danger/success)."
- src/lib/components/* — the font-mono text-[10px]/text-[11px] uppercase tracking-widest heading string recurs ~40+ times (55 matches for the pattern) as a bespoke class string across every component, with two variant sizes and occasional text-[10px] vs text-[11px] inconsistency (e.g. ApplicationDetailModal uses both). — medium — "Define a label-sm utility/component token (e.g. .heading-label or a @utility) and use it consistently."
- src/routes/dashboard/+page.svelte:327-337 vs src/routes/admin/approvals/+page.svelte:21-26 The "link buttons" share 90% of their class string but drift slightly (px-2.5 py-1.5 text-xs vs px-4 py-2 text-sm); Button.svelte already owns button sizing — these bypass it. — low — "Route link-buttons through Button or a shared link-button class."
- src/routes/+page.svelte:245, 250 vs src/lib/components/Button.svelte:70, 75 The auth submit button duplicates Button's entire markup + spinner (size-3 animate-spin rounded-full border border-current border-t-transparent) instead of using the <Button> component, and its label differs from the component vocabulary. — medium — "Replace with <Button …>."
Notable (non-flag) context
- +layout.svelte (9 lines) is clean: minimal, correct runes, no debt.
- export.csv/+server.ts — solid RFC-4180 escaping, BOM, no-store. Output notes worth flagging: the CSV emits enum/raw values (stage, status, workArrangement, minorUnits, timestamps) rather than the human-readable labels the docblock claims ("use the canonical display labels so the CSV is human-readable") — the values are the machine codes, so the stated intent and the output disagree. Also tags.join('|') uses | as a delimiter that isn't itself escaped if a tag contains |. — low — "Either map codes→labels to match the docblock, or adjust the docblock; escape the tag delimiter."
Highest-priority fixes: (1) the backslash bug at ApplicationDetailModal:45 (breaks modal self-close navigation), (2) unwired ApplicationForm values/errors/operation props (server validation never shows), (3) the permanently-dead disabled Upload handler at ResumeLibraryModal:172-176, (4) the role="link" <tr> a11y violation, and (5) missing prefers-reduced-motion.

---

Technical Debt Report — Job Tracker
A) src/routes/layout.css + design tokens
I read the entire 126-line file (plus all components referencing tokens). All @theme tokens are accounted for — there are no unused tokens (stage/status 100/700 pairs resolve through the StatusBadge/ConversionFunnel class maps; danger-fg via Button, danger-bg via ApplicationForm, accent-hover via Button/+error/+page, success via StatCard/ConversionFunnel). No duplicate token definitions. The prefers-color-scheme: dark block defines a dark value for every light token — no dark-mode token gaps there.
layout.css:9-13, 80-84 — No :root light/dark text-scaling baseline — medium.
The meta <meta name="text-scale" content="scale"> (app.html:6) implies a text-scaling root, but no :root { font-size: ... } or font-size: 100%/1rem baseline is set, and every size is an arbitrary text-[10px]/text-[11px] or fixed Tailwind size (no text-sm rem-scaling). Tiny mono labels won't scale with user preference. Fix: define a :root { font-size: 100% } and consider em/rem for labels, or drop the text-scale meta.
layout.css:78 — @custom-variant dark is dormant/dead — low.
The dark variant matches .dark/.dark * ancestors, but nothing ever adds a .dark class, and no dark: utility is used anywhere in src/ (verified by grep). Dark mode is actually driven only by the @media (prefers-color-scheme) :root:not(.light) block. Fix: remove the dormant variant (or wire an actual toggle) so it doesn't mislead.
No global prefers-reduced-motion handling — medium.
No motion-reduce: utility, @media (prefers-reduced-motion), or @media rule exists anywhere in src/ (grep: zero matches). Yet the UI ships animate-pulse (StatCard skeleton, StatCard.svelte:69), backdrop-blur-sm (Modal scrim, Modal.svelte:177), and transition-colors on ~30 interactive elements. This violates the AGENTS editorial/accessibility bar. Fix: add a global @media (prefers-reduced-motion: reduce) rule in layout.css disabling transition/animation and the modal blur.
FilterBar.svelte:126-133 — Hard-coded oklch() tag colors create a dark-mode gap — medium.
tagColors() returns inline oklch(0.95 0.02 ${b}) / oklch(0.42 0.12 ${b}) (light-background, dark-foreground) applied via style:background-color/style:color. In dark mode these stay light-bg/dark-fg pills that don't follow the token system — a real contrast/theme inconsistency. It also bypasses Tailwind tokens entirely (raw CSS color in component). Fix: promote tag chips to theme tokens (e.g. --color-tag-{n}-100/700) with dark variants, or gate the inline colors behind @custom-variant dark.
FilterBar.svelte:267-268 — Non-token inline-color arbitrary values float in a component — low.
The tag chip style:background-color / style:color are the only in-component hard-coded colors in the codebase (grep confirmed). They duplicate what the @theme token system is meant to own.
Non-scaled / inconsistent font sizes for the "mono uppercase label" role — medium.
The same editorial role (mono, uppercase, tracking-widest, muted) is rendered at mutually inconsistent sizes across components:
- text-[10px]: FilterBar.svelte:168,193,218,249,279; ApplicationDetailModal.svelte:134,198,202,206,214,222,228,234,247,271,438; ApplicationsTable.svelte:241,249; StageDwellChart.svelte:139,176,183; VelocityChart.svelte:240
- text-[11px]: StatCard.svelte:63; ApplicationsTable.svelte:60,75,82,...; StageDwellChart.svelte:83-84,94,123,159; ConversionFunnel.svelte:106,115-116,150,185; VelocityChart.svelte:145,148,158
- font-mono text-xs: +page.svelte:55 (auth "Job Tracker" label) and sort-by chips FilterBar.svelte:288
Same visual role, three size conventions. Fix: define one --text-label/label size utility or standardize on a single text-[11px] for mono labels and derive chart tick text from it.
Spacing/radius micro-inconsistencies — low.
- Pill padding differs: FilterBar chips px-2.5 py-1 (FilterBar.svelte:176,201,226,261) vs StatusBadge chips px-2 py-0.5 (StatusBadge.svelte:71) vs table tag chips px-2 py-0.5 (ApplicationsTable.svelte:241).
- Corner radius for the same "inline chip" object: rounded-full (pills) but table action hover uses rounded-sm squares and inputs rounded-md; visual radius vocabulary is only directionally consistent.
- Fix: consolidate a single chip/pill token set.
Fine on editorial design / off-white bg / thin borders / mono labels / single accent — no contradictions found beyond the token-size inconsistencies above. bg-bg/text-fg/bg-surface/border-border all align with the off-white/mono/thin-border/single-accent direction. No dead @media blocks (the only media block is the dark override, which is live).
B) URL-sync / shallow-routing state logic
ApplicationDetailModal.svelte:45 — Escaped \${qs} breaks detail-modal-close navigation — HIGH.
The literal \${qs} (verified by byte inspection) produces the string /dashboard?${qs} instead of interpolating qs. When ?app= is present alongside other params (filters, ?new, ?resume — onRowClick copies the full search +page.svelte:134-135), closing the detail modal navigates to a garbled URL and silently drops the filters. This is the only one of the three identical patterns (ResumeLibraryModal:32, dashboard:160) that is broken — both of those interpolate correctly, confirming it's an accidental escape. Fix: remove the backslash:  `/dashboard?${qs}` .
ApplicationDetailModal.svelte:107-111 — __openAppDetail window-global is dead code — medium.
A window.__openAppDetail = (id) => void pushState('', { appId: id }) is registered on every render but never invoked anywhere (grep: only the definition). It pollutes window, leaks a global, and its pushState('', ...) pushes a history entry that isn't mirrored/cleaned by the ?app= effect (which uses replaceState), so if it were ever used it would grow history. Fix: delete the block (and the now-unused pushState import at line 4).
+page.svelte:39-42 vs 70-82 — Dual $effect URL round-trip is fragile (server re-load on every keystroke) — medium.
The sync effect (39) writes filters/sort from data, and the nav effect (70) fires goto(...,{replaceState:true}) on any difference. Because goto performs a full client navigation that re-runs the server load for the whole dashboard on every committed filter change, each debounced keystroke and each chip click triggers a server round-trip (fetch + re-render of KPIs + charts + full table). This is a deliberate "URL as source of truth" design but is heavy; the comments even call it out as such. Fix: use pushState/history.replaceState (shallow) for filter/sort serialization and read on popstate, reserving goto for true route changes — or at minimum memoize/skip reload when only query (not pathname) changes.
+page.svelte:70-82 — Nav effect re-fires on every dependency change (no untrack) — low.
The $effect reads urlTarget, pageStore.url, and every data.* used indirectly; any unrelated data change can retrigger a redundant goto call (guarded only by string equality of the URL, so usually no-op but still a wasted effect tick + equality churn). Consider untrack on the comparison inputs.
+page.svelte:70,153 / ApplicationDetailModal:38 / ResumeLibraryModal:25 — Modal-close effects each run a goto on teardown via the bind:open derived — medium.
Each modal-close effect (dashboard isNewAppOpen effect at 153, detail at 38, resume at 25) is a separate goto(replaceState). With the writable-$derived bind:open pattern, closing one modal while another is open strips only its own param but performs its own navigation; combined with onRowClick's replaceState, focus/scroll/back behaviors are re-entrant and there is no single owner of URL writes. If the detail-modal bug above is fixed, two stacked modals (?app+?new) would each issue competing gotos. Fix: centralize all modal-param writes in the dashboard into one URL-navigation helper so only one goto per change fires.
Modal.svelte:10-17, 76-84, 153-163 — Esc close only handles the top modal and does not promote the next — medium.
topId !== id → return means Esc closes only the top modal. But on close, cleanup sets topId = null (Modal.svelte:156) and never re-assigns it to the modal below, so if two modals are open and you close the top one, Esc/capture stops working on the remaining one until some other interaction re-sets topId. Combined with the stack: ?app+?new/?resume can genuinely co-exist (each open fn copies the current search before adding its param). Fix: promote the next topId from openCount tracking on close, or disallow stacking modals in the URL.
Modal.svelte:169-186 — No inert/aria-hidden on background despite the comment — medium (a11y).
The comment claims "inert on the rest of the page ... is the modern primitive", but the code never applies inert (or aria-hidden) to the page behind the modal. Keyboard Tab is trapped by the focus cycle, but screen-reader/virtual-cursor users can still reach background content for all stacked modals, and there's a brief window before focus lands. Fix: apply inert to the app container (or aria-hidden to siblings) while a modal is open.
Modal.svelte:136-143 — Initial focus uses queueMicrotask with no dialogEl guard; stacked modals race focus — low/medium.
Both stacked modals run queueMicrotask(() => tabbables[0]?.focus()); the last to mount wins, and either can refocus if dialogEl briefly null. Focus restoration (restoreFocusEl?.focus() on close) also fires for the bottom modal when it closes, which can yank focus while the top modal is still open. Fix: only move focus on the top modal and only restore when it's the last open.
+page.svelte:133-141 — ?app= detail-open uses replaceState:true, defeating back-button — low.
Opening the detail modal replaces the current history entry, so Back cannot dismiss it or return to the prior filter state (the very "back-button-friendly" property the header comment at :26-27 claims). Combined with replaceState on every filter change, the Back button becomes nearly useless on the dashboard. Fix: decide on a consistent shallow-routing/history policy (e.g. pushState for modal open, replaceState for keystroke filters only).
Body scroll-lock stacking is handled correctly — confirmed no bug. Modal.svelte:10-11,145-147,158-160 uses a module-level openCount; first open locks document.body.style.overflow='hidden', last close unlocks. Module scope is shared across Modal instances, so stacked modals are counted properly.
FilterBar debounce — correct teardown, no leak — confirmed. FilterBar.svelte:28-41 clears the prior timer on each run and a cleanup clears it on unmount. The next !== filters.q guard prevents redundant writes. Minor: the effect re-runs when filters.q changes externally and re-arms a 150ms timer (FilterBar.svelte:29-37), which with the queryInput-stale-on-external-change gap (queryInput is captured once at setup, FilterBar.svelte:23) can briefly echo old text; low.
Zero-history-growth intent is honored for inline navigations. All goto calls use replaceState:true — no history growth from typing. (Separate note: form actions +page.server.ts:187,228,239,250,261,301,330 redirect(303, ...) each add a history entry, which is standard SvelteKit form behavior, not a regression.)
C) +layout.svelte / global body / app.html
app.html:2 — <html lang="en"> is correct — no finding.
<html lang="en"> present. Good.
app.html:10 — <body> classes are set in app.html, not layout.css — low.
class="min-h-screen bg-bg text-fg antialiased" (app.html:10) works, but it's a second place where design is declared (layout.css owns tokens + dark mode). If layout.css ever renders before app.html's body class swaps, there's a flash. Keep bg-bg/text-fg but consider pulling the base body style into layout.css for a single source of appearance.
+layout.svelte:1-3 — layout.css import order is safe — no finding.
import './layout.css' is the only import and @import 'tailwindcss' is the first line of layout.css, so tokens/custom-variant/dark block all load before components reference them. Safe.
+layout.svelte:8 — Favicon only; no font loading — low.
Only svelte:head content is the favicon. The --font-sans/--font-mono stacks reference system fonts (no network fetch), which is correct for a Workers free-tier/performance standpoint. The text-scale meta (app.html:6) implies some intent to scale text that nothing implements (see A above).
No <body> overscroll/smooth-scroll opinion — low. No global transition/smooth-scroll config; scrolling is default. Not a debt per se, but there's no guard against the modal body-scroll-lock interacting with 100dvh layouts (+page.svelte:52, +error.svelte:27 use min-h-[100dvh] while Modal uses the older 100vh-rooted pt-[8vh]; minor unit mismatch).
Severity summary
- High: ApplicationDetailModal.svelte:45 escaped \${qs} breaks closing the detail modal while filters are active.
- Medium: dead window.__openAppDetail global + unused pushState import; no global prefers-reduced-motion handling; hard-coded oklch() tag colors creating a dark-mode gap; inconsistent text-[10px]/text-[11px]/text-xs label sizing; full server goto round-trip on every filter keystroke; per-modal competing URL-write gotos; Esc doesn't re-promote stacked modal; missing inert/aria-hidden background trap.
- Low: dormant @custom-variant dark; body classes split between app.html and layout.css; dual $effect URL retrigger; replaceState defeating back-button; FilterBar queryInput staleness; redundant void STAGES/STATUSES keeps-alive imports; minor radius/padding/licence micro-inconsistencies.
No findings for: unused @theme tokens, dead @media blocks, light-only tokens (all have dark values), or no-navigation-without-resolve violations (all gotos correctly use resolvePath).

---
And as always, abide strictly by AGENTS.md, please use global agent relevant skills (you really need to check which global agent skills are available and load and use them don't just skip it), global agent ui/ux/style/design skills, use agent skills to its maximum fullet potentials, local agent research files (.agents/**), and mcp tools generously ya (always make sure you're full of knowledge from these local agent researchs files and mcp server tools docs, never assume you know or do it on your own accord without reading or using mcp docs). we should strictly follow AGENTS.md, .agents/** as our source of truths, mcp tools and mcp docs as our information sourcing and finding and double triple checking place. we must have rich visuals and informations, with very user friendliness, and graceful handlings. But no duplicate or redundant information should be seen And NEVER EVER disable deslint rules or do svelte ignore, always comply rules and best practices and standards and fix the issue to meet those.